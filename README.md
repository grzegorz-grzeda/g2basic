# g2basic
G2Labs BASIC interpreter

## To do
- separate parsing into handling just single lines of input
- save raw lines for listing and error handling (configurable)
- variables (int, float and string)
- basic let, print (string and variable content) and simple assignment 
- basic operations (+-*/^)
- evaluation of expressions for let
- evaluation of expressions for print
- if statement with conditions
- goto
- for loops with loop line number list
- gosub and stack of next line
- generic library API header
- generic keyword adding
- functions like SQRT(x), etc.